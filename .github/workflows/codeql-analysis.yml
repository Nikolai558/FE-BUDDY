# CodeQL security analysis for FE-BUDDY.
#
# Scans C# code on pushes and pull requests to the primary development
# and release branches, as well as on a weekly schedule.

name: "CodeQL"

on:
  workflow_dispatch:

  push:
    branches: [ development, releases ]

  pull_request:
    branches: [ development, releases ]

  schedule:
    - cron: '0 8 * * 1'

jobs:
  analyze:
    name: Analyze
    runs-on: windows-2025

    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'csharp' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.x'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: autobuild

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4