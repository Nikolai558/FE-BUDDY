<UserControl x:Class="FeBuddy.Wpf.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:FeBuddy.Wpf.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:PlaceholderViewModel, IsDesignTimeCreatable=True}"
             d:DesignWidth="820" d:DesignHeight="560">
    <Grid Background="{StaticResource Brush.Bg.Base}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="380">
            <TextBlock Style="{StaticResource Text.H1}" Text="{Binding Title}" HorizontalAlignment="Center" />
            <TextBlock Style="{StaticResource Text.Body}" Text="{Binding Blurb}"
                       TextAlignment="Center" Margin="0,8,0,0" />
            <Border Style="{StaticResource Chip}" HorizontalAlignment="Center" Margin="0,18,0,0">
                <TextBlock Style="{StaticResource Chip.Text}" Text="NOT BUILT YET" />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
